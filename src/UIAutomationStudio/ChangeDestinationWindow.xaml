<Window x:Class="UIAutomationStudio.ChangeDestinationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UIAutomationStudio"
        mc:Ignorable="d"
        Title="Change the Destination of this arrow" Height="200" Width="300" Loaded="OnLoaded" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition />
			<RowDefinition Height="40" />
		</Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Margin="5,10,5,0" TextWrapping="Wrap">Keeping this window open, select the action which you want this arrow to point to and after that press OK.</TextBlock>
		<Button x:Name="btnOK" Grid.Row="1" HorizontalAlignment="Right" Margin="0,0,120,0" Height="30" Width="100" IsDefault="True" Click="OnOK">OK</Button>
		<Button Grid.Row="1" HorizontalAlignment="Right" Margin="0,0,10,0" Height="30" Width="100" IsCancel="True" Click="OnCancel">Cancel</Button>
    </Grid>
</Window>
